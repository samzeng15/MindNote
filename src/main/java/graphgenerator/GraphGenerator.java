package graphgenerator;

import org.javatuples.Pair;

import java.util.ArrayList;

public class GraphGenerator {
    public static void CreateGraph(ArrayList<Pair<Integer, String>> input) {
        // convert word input into graph representation that can be converted into a visualisation in GraphVisualiser
    }
}
